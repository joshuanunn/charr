(include_subdirs unqualified)

(library
 (name subc)
 (modules
  io
  env
  cfg
  lexer
  lexer_pp
  parser
  type
  ast
  ast_resolution
  ast_flow_label
  ast_type_check
  irgen
  passes
  constant_folding
  copy_propagation
  dead_store_elimination
  unreachable_code_elimination
  ir
  codegen
  codegen_lower
  codegen_fixup
  asm
  emit)
 (preprocess
  (pps ppx_deriving.show)))

(menhir
 (modules parser))

(ocamllex lexer)
