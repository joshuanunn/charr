(Ir.Program
   [Ir.Function {name = "not"; global = true; params = ["ul.0"];
      body =
      [Ir.Unary {op = Ir.Not; src = (Ir.Var "ul.0");
         dst = (Ir.Var "not.tmp.0")};
        (Ir.Return (Ir.Var "not.tmp.0"))];
      frame =
      Env.lenv {
        namespace = "not";
        counter = 1;
        offset = 0;
        stack slots = {
        }}};
     Ir.Function {name = "if_cond"; global = true; params = ["u.1"];
       body =
       [Ir.JumpIfZero {condition = (Ir.Var "u.1"); target = "if_cond.if.en.0"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 1l)));
         (Ir.Label "if_cond.if.en.0");
         (Ir.Return (Ir.Constant (Ctype.ConstInt 0l)))];
       frame =
       Env.lenv {
         namespace = "if_cond";
         counter = 1;
         offset = 0;
         stack slots = {
         }}};
     Ir.Function {name = "and"; global = true; params = ["ul.2"; "i.3"];
       body =
       [Ir.JumpIfZero {condition = (Ir.Var "ul.2"); target = "and.and.fl.1"};
         Ir.JumpIfZero {condition = (Ir.Var "i.3"); target = "and.and.fl.1"};
         Ir.Copy {src = (Ir.Constant (Ctype.ConstInt 1l));
           dst = (Ir.Var "and.tmp.0")};
         Ir.Jump {target = "and.and.en.2"}; (Ir.Label "and.and.fl.1");
         Ir.Copy {src = (Ir.Constant (Ctype.ConstInt 0l));
           dst = (Ir.Var "and.tmp.0")};
         (Ir.Label "and.and.en.2"); (Ir.Return (Ir.Var "and.tmp.0"))];
       frame =
       Env.lenv {
         namespace = "and";
         counter = 3;
         offset = 0;
         stack slots = {
         }}};
     Ir.Function {name = "or"; global = true; params = ["i.4"; "u.5"];
       body =
       [Ir.JumpIfNotZero {condition = (Ir.Var "i.4"); target = "or.or.tr.1"};
         Ir.JumpIfNotZero {condition = (Ir.Var "u.5"); target = "or.or.tr.1"};
         Ir.Copy {src = (Ir.Constant (Ctype.ConstInt 0l));
           dst = (Ir.Var "or.tmp.0")};
         Ir.Jump {target = "or.or.en.2"}; (Ir.Label "or.or.tr.1");
         Ir.Copy {src = (Ir.Constant (Ctype.ConstInt 1l));
           dst = (Ir.Var "or.tmp.0")};
         (Ir.Label "or.or.en.2"); (Ir.Return (Ir.Var "or.tmp.0"))];
       frame =
       Env.lenv {
         namespace = "or";
         counter = 3;
         offset = 0;
         stack slots = {
         }}};
     Ir.Function {name = "main"; global = true; params = [];
       body =
       [Ir.FunCall {fun_name = "not";
          args = [(Ir.Constant (Ctype.ConstULong 1152921504606846976L))];
          dst = (Ir.Var "main.tmp.1")};
         Ir.JumpIfZero {condition = (Ir.Var "main.tmp.1");
           target = "main.if.en.2"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 1l)));
         (Ir.Label "main.if.en.2");
         Ir.FunCall {fun_name = "not";
           args = [(Ir.Constant (Ctype.ConstULong 0L))];
           dst = (Ir.Var "main.tmp.3")};
         Ir.Unary {op = Ir.Not; src = (Ir.Var "main.tmp.3");
           dst = (Ir.Var "main.tmp.4")};
         Ir.JumpIfZero {condition = (Ir.Var "main.tmp.4");
           target = "main.if.en.5"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 2l)));
         (Ir.Label "main.if.en.5");
         Ir.FunCall {fun_name = "if_cond";
           args = [(Ir.Constant (Ctype.ConstUInt -2147483648l))];
           dst = (Ir.Var "main.tmp.6")};
         Ir.Unary {op = Ir.Not; src = (Ir.Var "main.tmp.6");
           dst = (Ir.Var "main.tmp.7")};
         Ir.JumpIfZero {condition = (Ir.Var "main.tmp.7");
           target = "main.if.en.8"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 3l)));
         (Ir.Label "main.if.en.8");
         Ir.FunCall {fun_name = "if_cond";
           args = [(Ir.Constant (Ctype.ConstUInt 0l))];
           dst = (Ir.Var "main.tmp.10")};
         Ir.JumpIfZero {condition = (Ir.Var "main.tmp.10");
           target = "main.if.en.11"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 4l)));
         (Ir.Label "main.if.en.11");
         Ir.FunCall {fun_name = "and";
           args =
           [(Ir.Constant (Ctype.ConstULong 0L));
             (Ir.Constant (Ctype.ConstInt 1l))];
           dst = (Ir.Var "main.tmp.12")};
         Ir.JumpIfZero {condition = (Ir.Var "main.tmp.12");
           target = "main.if.en.13"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 5l)));
         (Ir.Label "main.if.en.13");
         Ir.FunCall {fun_name = "or";
           args =
           [(Ir.Constant (Ctype.ConstInt 1l));
             (Ir.Constant (Ctype.ConstUInt -2147483648l))];
           dst = (Ir.Var "main.tmp.14")};
         Ir.Unary {op = Ir.Not; src = (Ir.Var "main.tmp.14");
           dst = (Ir.Var "main.tmp.15")};
         Ir.JumpIfZero {condition = (Ir.Var "main.tmp.15");
           target = "main.if.en.16"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 6l)));
         (Ir.Label "main.if.en.16");
         (Ir.Return (Ir.Constant (Ctype.ConstInt 0l)))];
       frame =
       Env.lenv {
         namespace = "main";
         counter = 17;
         offset = 0;
         stack slots = {
         }}}
     ])
