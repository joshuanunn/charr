(Ir.Program
   [Ir.Function {name = "not"; global = true; params = ["l.0"];
      body =
      [Ir.Unary {op = Ir.Not; src = (Ir.Var "l.0"); dst = (Ir.Var "tmp.0")};
        (Ir.Return (Ir.Var "tmp.0"))];
      frame =
      Env.lenv {
        namespace = "not";
        counter = 1;
        offset = 0;
        stack slots = {
        }}};
     Ir.Function {name = "if_cond"; global = true; params = ["l.1"];
       body =
       [Ir.JumpIfZero {condition = (Ir.Var "l.1"); target = "if_cond.if.en.0"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 1l)));
         (Ir.Label "if_cond.if.en.0");
         (Ir.Return (Ir.Constant (Ctype.ConstInt 0l)))];
       frame =
       Env.lenv {
         namespace = "if_cond";
         counter = 1;
         offset = 0;
         stack slots = {
         }}};
     Ir.Function {name = "and"; global = true; params = ["l1.2"; "l2.3"];
       body =
       [Ir.JumpIfZero {condition = (Ir.Var "l1.2"); target = "and.and.fl.1"};
         Ir.JumpIfZero {condition = (Ir.Var "l2.3"); target = "and.and.fl.1"};
         Ir.Copy {src = (Ir.Constant (Ctype.ConstInt 1l));
           dst = (Ir.Var "tmp.0")};
         Ir.Jump {target = "and.and.en.2"}; (Ir.Label "and.and.fl.1");
         Ir.Copy {src = (Ir.Constant (Ctype.ConstInt 0l));
           dst = (Ir.Var "tmp.0")};
         (Ir.Label "and.and.en.2"); (Ir.Return (Ir.Var "tmp.0"))];
       frame =
       Env.lenv {
         namespace = "and";
         counter = 3;
         offset = 0;
         stack slots = {
         }}};
     Ir.Function {name = "or"; global = true; params = ["l1.4"; "l2.5"];
       body =
       [Ir.JumpIfNotZero {condition = (Ir.Var "l1.4"); target = "or.or.tr.1"};
         Ir.JumpIfNotZero {condition = (Ir.Var "l2.5"); target = "or.or.tr.1"};
         Ir.Copy {src = (Ir.Constant (Ctype.ConstInt 0l));
           dst = (Ir.Var "tmp.0")};
         Ir.Jump {target = "or.or.en.2"}; (Ir.Label "or.or.tr.1");
         Ir.Copy {src = (Ir.Constant (Ctype.ConstInt 1l));
           dst = (Ir.Var "tmp.0")};
         (Ir.Label "or.or.en.2"); (Ir.Return (Ir.Var "tmp.0"))];
       frame =
       Env.lenv {
         namespace = "or";
         counter = 3;
         offset = 0;
         stack slots = {
         }}};
     Ir.Function {name = "main"; global = true; params = [];
       body =
       [Ir.FunCall {fun_name = "not";
          args = [(Ir.Constant (Ctype.ConstLong 1152921504606846976L))];
          dst = (Ir.Var "tmp.0")};
         Ir.JumpIfZero {condition = (Ir.Var "tmp.0"); target = "main.if.en.1"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 1l)));
         (Ir.Label "main.if.en.1");
         Ir.FunCall {fun_name = "not";
           args = [(Ir.Constant (Ctype.ConstLong 0L))];
           dst = (Ir.Var "tmp.2")};
         Ir.Unary {op = Ir.Not; src = (Ir.Var "tmp.2");
           dst = (Ir.Var "tmp.3")};
         Ir.JumpIfZero {condition = (Ir.Var "tmp.3"); target = "main.if.en.4"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 2l)));
         (Ir.Label "main.if.en.4");
         Ir.FunCall {fun_name = "if_cond";
           args = [(Ir.Constant (Ctype.ConstLong 1152921504606846976L))];
           dst = (Ir.Var "tmp.5")};
         Ir.Unary {op = Ir.Not; src = (Ir.Var "tmp.5");
           dst = (Ir.Var "tmp.6")};
         Ir.JumpIfZero {condition = (Ir.Var "tmp.6"); target = "main.if.en.7"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 3l)));
         (Ir.Label "main.if.en.7");
         Ir.FunCall {fun_name = "if_cond";
           args = [(Ir.Constant (Ctype.ConstLong 0L))];
           dst = (Ir.Var "tmp.8")};
         Ir.JumpIfZero {condition = (Ir.Var "tmp.8"); target = "main.if.en.9"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 4l)));
         (Ir.Label "main.if.en.9");
         Ir.FunCall {fun_name = "and";
           args =
           [(Ir.Constant (Ctype.ConstLong 0L));
             (Ir.Constant (Ctype.ConstInt 1l))];
           dst = (Ir.Var "tmp.10")};
         Ir.JumpIfZero {condition = (Ir.Var "tmp.10");
           target = "main.if.en.11"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 5l)));
         (Ir.Label "main.if.en.11");
         Ir.FunCall {fun_name = "or";
           args =
           [(Ir.Constant (Ctype.ConstInt 1l));
             (Ir.Constant (Ctype.ConstLong 1152921504606846976L))];
           dst = (Ir.Var "tmp.12")};
         Ir.Unary {op = Ir.Not; src = (Ir.Var "tmp.12");
           dst = (Ir.Var "tmp.13")};
         Ir.JumpIfZero {condition = (Ir.Var "tmp.13");
           target = "main.if.en.14"};
         (Ir.Return (Ir.Constant (Ctype.ConstInt 6l)));
         (Ir.Label "main.if.en.14");
         (Ir.Return (Ir.Constant (Ctype.ConstInt 0l)))];
       frame =
       Env.lenv {
         namespace = "main";
         counter = 15;
         offset = 0;
         stack slots = {
         }}}
     ])
