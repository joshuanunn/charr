(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "main"); params = [];
         body = (Some (Ast.Block []));
         fun_type = Ctype.FunType {params = []; ret = Ctype.Int};
         storage = None })
     ])
