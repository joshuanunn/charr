(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "main"); params = [];
         body =
         (Some (Ast.Block
                  [(Ast.D
                      (Ast.VarDecl
                         { Ast.name = (Ast.Identifier "to_divide.0");
                           init =
                           (Some { Ast.e = (Ast.Constant (Ctype.ConstInt 8l));
                                   typ = (Some Ctype.Int) });
                           var_type = Ctype.Int; storage = None }));
                    (Ast.S
                       (Ast.Expression
                          { Ast.e =
                            (Ast.Assignment (
                               { Ast.e =
                                 (Ast.Var (Ast.Identifier "to_divide.0"));
                                 typ = (Some Ctype.Int) },
                               { Ast.e =
                                 Ast.Binary {op = Ast.Divide;
                                   left =
                                   { Ast.e =
                                     (Ast.Var (Ast.Identifier "to_divide.0"));
                                     typ = (Some Ctype.Int) };
                                   right =
                                   { Ast.e =
                                     (Ast.Constant (Ctype.ConstInt 4l));
                                     typ = (Some Ctype.Int) }};
                                 typ = (Some Ctype.Int) }
                               ));
                            typ = (Some Ctype.Int) }));
                    (Ast.S
                       (Ast.Return
                          { Ast.e = (Ast.Var (Ast.Identifier "to_divide.0"));
                            typ = (Some Ctype.Int) }))
                    ]));
         fun_type = Ctype.FunType {params = []; ret = Ctype.Int};
         storage = None })
     ])
