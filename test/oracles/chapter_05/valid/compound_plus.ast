(Ast.Program
   [(Ast.FunDecl
       { Ast.name = (Ast.Identifier "main"); params = [];
         body =
         (Some (Ast.Block
                  [(Ast.D
                      (Ast.VarDecl
                         { Ast.name = (Ast.Identifier "to_add");
                           init = (Some (Ast.Constant (Ast.ConstInt 0l)));
                           var_type = Ast.Int; storage = None }));
                    (Ast.S
                       (Ast.Expression
                          (Ast.Assignment (
                             (Ast.Var (Ast.Identifier "to_add")),
                             Ast.Binary {op = Ast.Add;
                               left = (Ast.Var (Ast.Identifier "to_add"));
                               right = (Ast.Constant (Ast.ConstInt 4l))}
                             ))));
                    (Ast.S (Ast.Return (Ast.Var (Ast.Identifier "to_add"))))]));
         fun_type = Ast.FunType {params = []; ret = Ast.Int}; storage = None
         })
     ])
